PORT=4000
# Optional single URL for production deployments like Railway or Render.
# Example: mysql://user:password@host:3306/database_name
DATABASE_URL=
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=secret
DB_NAME=verified_rental
JWT_SECRET=change_this_secret
